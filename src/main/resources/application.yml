embabel:
  agent:
    logging:
      personality: starwars
    platform:
      toolloop:
        type: parallel
  models:
    defaultLlm: claude-sonnet-4-5

faction:
  github:
    token: ${FACTION_GITHUB_TOKEN:}

database:
  dataSources:
    factions:
      type: NEO4J
      userName: ${NEO4J_USERNAME:neo4j}
      password: ${NEO4J_PASSWORD:brahmsian}
      host: ${NEO4J_HOST:localhost}
      port: 7687
      databaseName: factions

logging:
  level:
    org.drivine: WARN
    com.embabel.faction: INFO
    com.embabel.faction.agent.ExodusDetector: DEBUG
